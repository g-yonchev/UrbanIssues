<UserControl
    x:Class="UrbanIssues.Views.HeaderTitle"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UrbanIssues.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<Grid Name="panelTitle">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="60"/>
			<ColumnDefinition Width="1*"/>
		</Grid.ColumnDefinitions>
		<Grid Name="panelTitleLogo" 
				  Background="Azure"
				  Grid.Column="0">
			<Image
				Name="logo"
				Style="{StaticResource LogoStyle}"/>
		</Grid>
		<Grid Name="panelTitleText" 
				  Background="Brown"
				  Grid.Column="1">
			<TextBlock Name="textBlockTitle"
					   Text="{StaticResource TitleText}"
					   Style="{StaticResource TitleStyle}"/>
		</Grid>
	</Grid>
</UserControl>
